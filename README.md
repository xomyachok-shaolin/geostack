# 🌍 GeoStack

Интерактивный 3D веб-просмотрщик геопространственных данных на базе CesiumJS и Next.js с интеграцией данных из OpenStreetMap и Росреестра (НСПД).

![GeoStack](https://img.shields.io/badge/Next.js-14-black) ![CesiumJS](https://img.shields.io/badge/CesiumJS-1.114-blue) ![TypeScript](https://img.shields.io/badge/TypeScript-5-blue)

## ✨ Возможности

- **3D Тайлы** — просмотр 3D моделей зданий в формате 3D Tiles (b3dm)
- **Множество подложек** — ArcGIS, Cesium Ion, Google Satellite, OpenStreetMap
- **Cesium World Terrain** — глобальный рельеф местности
- **Русские надписи** — поддержка Google Hybrid с русским языком
- **Информация о зданиях** — клик по зданию показывает данные из:
  - **OpenStreetMap** — адрес, этажность, год постройки, материалы
  - **Nominatim** — геокодирование и детали адреса
  - **НСПД Росреестра** — кадастровые данные, площадь, стоимость
- **Кадастровые номера** — копирование и ссылки на ПКК/НСПД
- **Сохранение настроек** — выбранная модель и подложка сохраняются в браузере
- **Сворачиваемая панель** — минималистичный интерфейс

## 🚀 Быстрый старт

### Требования

- Node.js 18+
- pnpm (рекомендуется) или npm

### Установка

```bash
# Клонировать репозиторий
git clone https://github.com/xomyachok-shaolin/geostack.git
cd geostack

# Установить зависимости
pnpm install

# Запустить в режиме разработки
pnpm dev
```

Откройте http://localhost:3000 в браузере.

> **Примечание:** Скрипт `setup:cesium` запускается автоматически после установки зависимостей.

### Production сборка

```bash
pnpm build
pnpm start
```

## 📁 Структура проекта

```
geostack/
├── src/
│   ├── app/
│   │   ├── api/building-data/    # API для данных о зданиях
│   │   ├── globals.css
│   │   ├── layout.tsx
│   │   └── page.tsx
│   ├── components/
│   │   ├── CesiumViewer.tsx      # Основной 3D просмотрщик
│   │   ├── InfoPanel.tsx         # Панель информации о здании
│   │   └── Toolbar.tsx           # Панель управления
│   └── lib/
│       ├── api/                  # API клиенты
│       │   └── building-data-client.ts
│       ├── config/               # Конфигурация
│       │   └── cesium-config.ts
│       ├── types/                # TypeScript типы
│       │   └── index.ts
│       └── utils/                # Утилиты
│           ├── constants.ts
│           └── imagery-providers.ts
├── data/
│   ├── models/                   # 3D Tiles модели
│   │   ├── krasnoarmeiskoe/
│   │   └── kanash/
│   └── terrain/                  # DEM файлы рельефа
├── docker/                       # Docker конфигурация
│   ├── deploy.sh
│   ├── docker-compose.yml
│   └── docker-compose.prod.yml
├── public/
│   ├── cesium/                   # Статические файлы CesiumJS
│   └── models -> ../data/models  # Симлинк
├── scripts/
│   └── nspd-fetcher.py  # Скрипт для загрузки данных НСПД
└── ...
```

## ⛰️ Локальный рельеф (DEM)

В `data/terrain` лежат два GeoTIFF DEM в UTM:

- `dem_UTM_Kanash.tif`
- `dem_UTM_Krasnoarmeiskoe.tif`

Они используются для выравнивания 3D моделей по высоте относительно реального рельефа.
API для получения высоты в точке:

```
GET /api/dem-height?lon=47.525&lat=55.507&name=Kanash
GET /api/dem-height?lon=47.171&lat=55.770&name=Krasnoarmeiskoe
```

Ответ:

```json
{ "height": 194.3, "source": "Kanash" }
```

Если точка вне покрытия DEM или попала в `noData`, вернётся `height: null`.

## 🗺️ Поддерживаемые подложки

| Подложка | Описание |
|----------|----------|
| ArcGIS Спутник | ESRI World Imagery (по умолчанию) |
| Cesium Ion Спутник | Bing Maps Aerial через Cesium Ion |
| Google Спутник | Google Satellite без надписей |
| Спутник + Надписи (RU) | Google Hybrid с русскими надписями |
| OpenStreetMap | Векторная карта OSM |

## 🏢 Источники данных о зданиях

При клике на здание система запрашивает данные из трёх источников:

| Источник | Данные |
|----------|--------|
| **OSM Overpass** | Адрес, этажность, год постройки, материалы, организации |
| **Nominatim** | Полный адрес, город, регион, почтовый индекс |
| **НСПД Росреестра** | Кадастровый номер, площадь, стоимость, назначение, статус |

Данные кэшируются на сервере (10 мин) и клиенте (1 мин) для оптимизации.

## ⚙️ Конфигурация

### Cesium Ion Token

Для работы Cesium Ion подложек и рельефа требуется токен:

1. Зарегистрируйтесь на [cesium.com](https://cesium.com/ion/signup)
2. Получите токен на [cesium.com/ion/tokens](https://cesium.com/ion/tokens)
3. Замените токен в `src/lib/cesium-config.ts`

### Добавление 3D моделей

Поместите tileset.json и b3dm файлы в папку `models/` и добавьте запись в `AVAILABLE_MODELS` в `src/lib/cesium-config.ts`:

```typescript
export const AVAILABLE_MODELS: Model3D[] = [
  {
    id: 'my-model',
    name: 'Моя модель',
    url: '/models/my-model/tileset.json',
    center: {
      longitude: 47.15,
      latitude: 55.72,
      height: 200,
    },
  },
];
```

## 🐳 Docker

```bash
# Сборка образа
docker build -t geostack .

# Запуск контейнера
docker run -p 3000:3000 geostack
```

## 📝 Скрипты

| Команда | Описание |
|---------|----------|
| `pnpm dev` | Запуск в режиме разработки |
| `pnpm build` | Production сборка |
| `pnpm start` | Запуск production сервера |
| `pnpm setup:cesium` | Копирование статических файлов Cesium |
| `pnpm clean` | Очистка кэша сборки |
| `pnpm rebuild` | Полная пересборка |

## 🛠️ Технологии

- **[Next.js 14](https://nextjs.org/)** — React фреймворк с App Router
- **[CesiumJS 1.114](https://cesium.com/cesiumjs/)** — 3D геопространственная библиотека
- **[TypeScript 5](https://www.typescriptlang.org/)** — типизация
- **[pnpm](https://pnpm.io/)** — менеджер пакетов

### Внешние API

- **[Overpass API](https://overpass-api.de/)** — данные OpenStreetMap
- **[Nominatim](https://nominatim.openstreetmap.org/)** — геокодирование
- **[НСПД Росреестра](https://nspd.gov.ru/)** — кадастровые данные
